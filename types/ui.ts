// UI-specific shared types
// Color tokens map to CSS variables defined in `app/globals.css`.
// Keep this list in sync with Convex hueValidator in `convex/validators.ts`.

export type Color = 'default' | 'gray' | 'brown' | 'orange' | 'yellow' | 'green' | 'blue' | 'purple' | 'pink' | 'red';
